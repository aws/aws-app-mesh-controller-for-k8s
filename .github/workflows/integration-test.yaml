name: integration-test
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    name: integration-test
    runs-on: ubuntu-18.04
    # runs-on: [self-hosted, aws-app-mesh-controller-for-k8s, X64 ]
    permissions:
      id-token: write
      contents: read
    steps:
      - name: clean work dir from previous runs
        run: |
          rm -rf *
      - name: configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: arn:aws:iam::115811320858:role/AWSAppMeshControllerGithubRunner
          aws-region: us-west-2
      - name: setup go 1.16
        uses: actions/setup-go@v3
        with:
          go-version: '1.16.*'
        id: go
      - name: install controller-gen
        run: |
          go get sigs.k8s.io/controller-tools/cmd/controller-gen@v0.4.1
      - name: setup environment
        run: |
          source ~/.bashrc
      - name: checkout code
        uses: actions/checkout@v2
      - name: setup kind and run integration tests
        run: make integration-test
  cleanup:
    runs-on: ubuntu-18.04
    # runs-on: [self-hosted, aws-app-mesh-controller-for-k8s, X64]
    if: ${{ always() }}
    needs: [build]
    steps:
      - name: delete kind clusters
        run: make delete-all-kind-clusters
